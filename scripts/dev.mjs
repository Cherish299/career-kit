#!/usr/bin/env node
/* scripts/dev.mjs — 开发启动入口（占位：后端就绪后接线）
 * 规划：services/api（uvicorn）+ apps/web（vite/next）并行启动
 */
process.stdout.write(
  "CareerOS dev 尚未接线：后端骨架在 Day 4（FastAPI + PostgreSQL），前端在 Day 5。\n" +
  "当前可用：npm run build / npm run test（legacy 工具）。\n"
);
