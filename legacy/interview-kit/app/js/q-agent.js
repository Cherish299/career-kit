/* q-agent.js — 定制题库（Agent 与工具调用 + LLM 应用）
 * 针对简历定制：AI 应用开发 / Agent 应用 / RAG 后端
 * 覆盖 EcoMINER 项目核心工程：多阶段 Agent 工作流、结构化输出、
 * Schema 校验、重试降级、审核队列、多智能体协作。
 */
(function (global) {
  "use strict";
  var Q = global.INTERVIEW_QUESTIONS = global.INTERVIEW_QUESTIONS || [];

  function add(cat, type, diff, q, solution, extra) {
    var item = Object.assign({ id: "", cat: cat, type: type, diff: diff, q: q, solution: solution, tags: [] }, extra || {});
    item.id = cat + "-" + String(Q.length + 1).padStart(3, "0");
    Q.push(item);
  }

  /* ==================== Agent 与工具调用 (agent) ==================== */

  add("agent", "short", 2,
    "什么是 ReAct 模式？推理（Reasoning）与行动（Action）如何交替？",
    "ReAct 让 LLM 交替输出 Thought（推理：分析当前状态、决定下一步）→ Action（行动：调用工具/检索/查询）→ Observation（观察：工具返回结果）→ 循环直到 Task Complete。相比直接问答，ReAct 把\"思考过程\"外显，让模型能基于工具反馈修正计划，解决多步推理与外部信息获取问题。实现要点：prompt 里定义 Thought/Action/Action Input/Observation 格式；工具结果拼接回上下文；设置最大步数防止死循环。",
    { tags: ["ReAct", "推理", "Agent"] });

  add("agent", "short", 2,
    "Function Calling（工具调用）的原理是什么？与 JSON Schema 有什么关系？",
    "原理：把可用工具描述（名称、参数 Schema）注入系统提示，模型在需要时输出一个结构化\"函数调用请求\"（函数名 + 参数 JSON），由应用侧执行真实函数并把结果返回给模型继续推理。JSON Schema 是工具参数的声明格式（类型、必填、枚举、嵌套结构）——模型按 Schema 约束生成参数，应用按 Schema 校验参数合法性，两者配合实现\"LLM 决策 + 代码执行\"。主流实现：OpenAI function calling、Anthropic tool use；兼容做法：提示词 + JSON mode 自解析。",
    { tags: ["Function Calling", "JSON Schema", "工具调用"] });

  add("agent", "choice", 1,
    "要让 LLM 输出严格可解析的 JSON，最可靠的方式是：",
    "使用模型的原生结构化输出能力（function calling / JSON mode / structured output）最可靠——模型在解码层约束输出格式，从源头保证合法 JSON；提示词\"请输出 JSON\"只是软约束，模型仍可能输出多余文字或格式错误。拿到输出后仍应做 Schema 校验与解析兜底（如 JSON 修复/重试），形成双保险。",
    { options: ["A. 在提示词里写\"请输出 JSON\"", "B. 使用 function calling / structured output 并由应用侧做 Schema 校验", "C. 让模型输出后人工整理", "D. 用正则强行截取"], answer: "B", tags: ["结构化输出", "JSON"] });

  add("agent", "short", 2,
    "JSON Mode / Structured Output 与 Function Calling 有什么区别？",
    "JSON Mode / Structured Output：约束模型输出为合法 JSON（可按给定 JSON Schema 约束字段类型），输出直接是数据，没有\"函数名\"概念，适合数据提取/格式化场景（如从文本抽字段）。Function Calling：模型输出的是\"调用哪个函数 + 参数\"，执行权在应用（真实函数），强调与外部系统交互；实现上函数参数也走 JSON Schema。选型：纯数据抽取（如文献字段提取）用 Structured Output；需要联动工具/系统（如查库、调接口）用 Function Calling。",
    { tags: ["JSON Mode", "Function Calling", "区别"] });

  add("agent", "short", 2,
    "多阶段 Agent 工作流中，\"阶段\"之间如何传递中间产物？设计上要注意什么？",
    "中间产物传递：每阶段输出统一为结构化记录（如 StageResult { stage, status, payload, meta }），payload 是阶段数据（Pydantic 模型），meta 含来源锚点、置信度、耗时；下游阶段读取前序产物作为输入。注意：① 统一阶段契约（输入输出 Schema 一致，便于编排与复用）；② 中间产物持久化（数据库/对象存储）并关联任务 ID，支持断点续跑与失败重试；③ 每阶段带结构化状态（pending/running/success/failed/needs_review）；④ 全链路日志与产物快照，便于定位\"哪一步丢了什么\"；⑤ 结果审核信息随产物流转（低置信度标记进人审队列）。",
    { tags: ["多阶段", "中间产物", "工作流"] });

  add("agent", "short", 3,
    "Agent 执行失败（模型异常输出、工具报错、超时）时，如何设计重试与降级策略？",
    "① 分层重试：瞬时错误（网络、限流）指数退避重试（如 3 次）；模型输出校验失败（Schema 不通过）先重试 1-2 次（换温度或换提示），仍失败转规则兜底；② 降级路径：主模型失败→备用模型/小模型；VLM 失败→OCR/规则抽取兜底；LLM 抽取失败→标记 needs_review 进人工队列，不硬凑结果；③ 超时控制：单次调用超时 + 全链路超时（如单阶段 60s，总任务 10min），配合任务状态机让用户可查询；④ 幂等：工具/阶段幂等设计，重试不产生重复副作用；⑤ 观测：失败原因结构化记录（错误码、阶段、输入快照），便于复盘优化。核心原则：失败可感知、可重试、有兜底、不静默出错。",
    { tags: ["重试", "降级", "容错"] });

  add("agent", "short", 2,
    "什么情况下需要\"审核队列\"（human-in-the-loop）？如何设计？",
    "需要人工审核的场景：① 模型输出置信度低或校验失败（数值抽取精度存疑）；② 错误成本高（入库数据要用于科研/生产）；③ 敏感/合规内容；④ 新领域冷启动（标注样本训练评估）。设计：每阶段产物带置信度/风险标记；低于阈值的进审核队列，附带上下文快照（原始输入、模型输出、校验失败原因）便于人快速判断；人工可改、可驳回、可确认入库；审核结果回流入库并作为评估/微调样本。控制人审比例（如 <10%）以平衡成本——通过阈值与提示优化逐步降低人审率。",
    { tags: ["审核队列", "HITL", "人工审核"] });

  add("agent", "short", 2,
    "Agent 的\"记忆\"有哪几种？各自用途？",
    "① 短期记忆（对话内）：当前会话的上下文窗口，用于多轮交互；② 长期记忆（跨会话）：用户偏好、历史事实，可存数据库/向量库，按需检索注入；③ 工作记忆（任务内）：中间产物、工具结果、待办清单，随任务流转；④ 程序化记忆：技能/工具/流程定义（如阶段脚本、知识库）。记忆管理的核心挑战是上下文窗口有限——需摘要、检索、裁剪来取舍信息。Agent 应用（如个人助手）中长期记忆常配合向量检索实现\"记住用户\"。",
    { tags: ["记忆", "上下文", "Agent"] });

  add("agent", "short", 3,
    "如何把一个复杂任务分解给多智能体？多智能体协作有哪些模式？",
    "分解思路：按阶段（解析→抽取→审核）、按数据分片（分论文/分图）、按职责（规划者/执行者/审核者）拆分，每步输出统一契约，避免耦合。协作模式：① 编排式（Orchestrator-Workers）：主 Agent 规划任务、分发给子 Agent 并行执行、汇总结果——可控性强，适合流水线；② 协作式（对话/黑板）：Agent 间自由通信共享状态——灵活但难调试；③ 层级式：规划 Agent 下挂执行 Agent 树。工程上推荐编排式：主控负责任务分解、失败重试、结果聚合，子 Agent 只做单一职责。多智能体并非越多越好——单 Agent + 工具链往往已够，引入多 Agent 需权衡协调成本。",
    { tags: ["多智能体", "编排", "任务分解"] });

  add("agent", "choice", 2,
    "工具调用循环中，防止 LLM 反复调用同一工具/无限循环的机制是：",
    "工程上组合使用：① 最大步数限制（如 10 轮）与总时间预算；② 重复调用检测——相同工具+相同参数连续多次则终止或告警；③ 工具结果去重缓存（同一查询结果不再重复执行）；④ 结果无变化判定（连续 N 轮 Observation 相同则停止）；⑤ 强工具错误后重试次数上限。单项机制都不够，需多层防护。",
    { options: ["A. 只靠提示词说明", "B. 最大步数 + 重复检测 + 工具结果缓存等多层限制", "C. 加大模型上下文", "D. 取消工具调用能力"], answer: "B", tags: ["死循环", "工具调用", "防护"] });

  add("agent", "short", 2,
    "为什么工具调用/抽取类任务通常使用较低 temperature（如 0-0.3）？",
    "temperature 控制采样随机性：越低越接近贪心（greedy），输出更确定、可复现；越高越多样、有创造性。工具调用、结构化抽取、代码生成任务要求输出精确符合 Schema、参数准确，随机性只会增加格式错误与幻觉风险，故用低温度。创意写作、头脑风暴才用高温度。注意：即使 temperature=0，模型输出也不绝对确定（采样与并行计算），关键路径仍需校验兜底。",
    { tags: ["temperature", "采样", "确定性"] });

  add("agent", "short", 3,
    "设计一个\"论文 → 结构化数据 → 知识图谱\"的多智能体流水线，说明角色划分与失败处理。",
    "角色划分：① Orchestrator（编排）：接收 PDF 任务，拆分子任务、跟踪状态、聚合结果、失败重试；② Parser（解析）：PDF→文本/表格/图表区域（PyMuPDF/版面分析）；③ Extractor（抽取）：对正文/表格用 LLM 抽实体属性，对图表用 VLM 抽数值（Pydantic 约束输出）；④ Filler（回填）：RAG 检索图注/正文补全缺失字段，记录证据；⑤ Validator（校验）：Schema + 规则校验（单位、数值范围、误差），低置信度标记；⑥ GraphImporter（图谱）：校验通过记录按 OBOE 映射为 Observation/Measurement 写入 Neo4j，建立溯源边；⑦ Reviewer（人审）：处理 needs_review 产物。失败处理：每阶段状态机 + 重试（瞬时错误退避重试，校验失败换提示重试，超时降级）+ 幂等写入 + 全链路日志；抽取质量不过阈值不硬入库。",
    { tags: ["多智能体", "流水线", "知识图谱", "系统设计"] });

  add("agent", "short", 2,
    "上下文窗口有限时，Agent 如何处理长文档/长对话的上下文？",
    "① 分块处理：长文档切片后逐块处理，只保留与任务相关的块（检索式注入）；② 摘要压缩：历史对话/长文档滚动摘要，替换原始内容；③ 分层记忆：热数据（当前任务）进上下文，冷数据（历史事实）进向量库按需检索；④ 结构化抽取：把长文档先抽成结构化数据（字段/表格），只把结构化结果放上下文；⑤ 工具化：不把全文塞给模型，而是提供检索工具，模型按需查（RAG/数据库查询）；⑥ 上下文规划：Agent 主动决定丢弃/归档不相关信息。核心原则：上下文是稀缺资源，按需注入而非全量加载。",
    { tags: ["上下文管理", "长文档", "摘要"] });

  add("agent", "judge", 1,
    "用 Pydantic/JSON Schema 校验 LLM 输出是保证下游代码不崩溃的唯一手段。",
    "错。Schema 校验是必要但不充分：它只能保证结构合法，不能保证内容正确（数值读错、字段语义错误照样通过校验）。可靠方案是\"结构校验 + 内容规则校验（范围/单位/一致性）+ 置信度评估 + 人工兜底\"多层组合；同时调用端还要处理超时、重试、异常降级，才能保证下游稳定。",
    { answer: "错", tags: ["Schema 校验", "误区"] });

  add("agent", "short", 2,
    "Prompt 中\"系统提示词\"与\"few-shot 示例\"各自的作用？什么时候需要 few-shot？",
    "系统提示词：定义角色、任务目标、输出格式、约束与边界（如\"只依据给定材料回答\"），作用于每次调用，负责\"立规矩\"。few-shot：在 prompt 中给若干输入-输出示例，让模型模仿示例的模式（格式、粒度、风格），负责\"给范例\"。需要 few-shot 的场景：输出格式复杂（结构化抽取）、领域专有表达（科研术语归一化）、任务边界模糊（需要示例界定什么该抽什么不该抽）。一般先写好系统提示词，示例放在 few-shot 区（对话历史或示例段），注意示例要覆盖典型与边界情况，数量 2-5 个即可。",
    { tags: ["Prompt", "few-shot", "系统提示"] });

  add("agent", "short", 3,
    "如何评估一个多智能体抽取系统端到端的质量？",
    "分层评估：① 每阶段单独评估（解析：表格/图表检出率；抽取：字段级 precision/recall/F1，数值误差 MAE）；② 端到端：黄金标注集（人工标注 N 条完整记录）上对比系统输出，算记录级完全匹配率、字段级准确率、数值误差分布；③ 质量指标：人审比例（越低越好）、重试率、失败率、审核通过率；④ 检索质量：Recall@K、Rerank 命中率；⑤ 业务指标：入库数据可用率、下游查询命中率。评估集要覆盖不同文献类型/图表复杂度；定期用新标注样本回归，防止提示词改动引入回退。",
    { tags: ["评估", "黄金集", "质量"] });

  /* ==================== LLM 应用补充 (llm) ==================== */

  add("llm", "short", 1,
    "LLM 应用中的流式输出（SSE）有什么意义？实现上要注意什么？",
    "意义：① 首 token 延迟大幅降低（TTFT），用户体验好（不用等完整生成）；② 长回答可边生成边展示，降低等待焦虑；③ 可配合中断（用户停止生成）。实现：后端用 SSE（text/event-stream）逐 chunk 推送（或 WebSocket），前端 EventSource/fetch stream 逐段渲染；注意：① chunk 可能在 token 中间截断（UTF-8 多字节字符需缓冲拼接）；② 增量渲染 Markdown 需防闪烁（可用临时拼接+防抖）；③ 断线重连与流终止信号（[DONE]）；④ 流式时无法整体校验 JSON——结构化输出场景可先流式展示、最终校验。",
    { tags: ["流式", "SSE", "TTFT"] });

  add("llm", "short", 2,
    "如何估算与控制 LLM 调用的 token 成本？prefix caching 的原理是什么？",
    "估算：token 数 ≈ 输入（系统提示+few-shot+检索内容+历史）× 单价 + 输出 × 单价；可先离线按字符数粗估（中文约 1 字≈1-2 token），上线后按实际用量计费监控（每次调用记录 prompt/completion token 数与成本仪表盘）。控制：压缩检索内容（只保留相关片段）、控制 top-k、历史摘要化、减少重复系统提示、缓存结果。prefix caching（如 DeepSeek 的上下文硬盘缓存）：相同前缀（系统提示、长 few-shot）命中缓存，只按增量 token 计费并降低 prefill 延迟——把不变的长前缀放在 prompt 开头并保持稳定即可受益。",
    { tags: ["token 成本", "prefix caching", "优化"] });

  add("llm", "choice", 2,
    "同一份上下文反复命中缓存时，DeepSeek 的上下文硬盘缓存计费特点是：",
    "命中缓存的输入 token 价格远低于未命中（如 0.1 元/百万 vs 1 元/百万量级），且 prefill 延迟显著下降。规律：稳定长前缀（系统提示、few-shot、共享文档片段）复用越多越省。工程配合：保持公共前缀稳定、把易变内容放后面、长任务复用同一会话上下文。",
    { options: ["A. 与未命中完全同价", "B. 命中部分输入按大幅折扣计价，且 prefill 更快", "C. 只有输出 token 计费", "D. 缓存只对长文生效一次"], answer: "B", tags: ["缓存计费", "DeepSeek"] });

})(typeof window !== "undefined" ? window : globalThis);
